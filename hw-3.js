//1
let password = 'пароль';
let ask = prompt ('Введите пароль');
if ( ask === password ) {
    console.log ('Пароль введен верно');
} else {
    console.log ('Пароль введен неправильно');
}
//2
let c = 5;
if (c > 0 && c < 10 ) {
    console.log ('Верно')
} else {
    console.log ('Неверно')
}
